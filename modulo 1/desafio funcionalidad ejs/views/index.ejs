
<!DOCTYPE html>
<html lang="en">
    <%- include('./views/header.ejs') %>
    <body class="p-4">
        <div class="container bg-light p-4">
            <section>
                <h2 class="text-primary">Lista Productos</h2>
                <%- include('./views/formularioProds.ejs',{ metodo:'POST', action:'/productos'} )%>
            </section>
            <section class="mt-5">
                <h2 class="text-danger">Historial</h2>
                <% if(!productos.length) { %>
                    <h4 class="text-bg-danger p-3">producto no encontrado</h4>
                <% } else { %>
                    <table class="table table-dark">
                        <thead>
                            <tr class="text-warning">
                                <th>Título</th>
                                <th>Precio</th>
                                <th>Miniatura</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% productos.forEach(producto=> { %>
                                <tr>
                                    <td>
                                        <%= this.title %>
                                    </td>
                                    <td>
                                        <%= this.price %>
                                    </td>
                                    <td>
                                        <img height="60px" width="60px" src= <%= this.thumbnail %> />
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                <% } %>
            </section>
        </div>
    </body>
</html>
